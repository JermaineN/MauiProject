<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MoviesMauiApp.ViewModels"
             x:Class="MoviesMauiApp.Views.StartupPage"
             Title="Welcome">
    
    <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">
        
        <Label Text="🎬 Movie Explorer"
               FontSize="32"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <Label Text="Please enter your name to get started:"
               FontSize="18"
               HorizontalOptions="Center" />

        <Entry Placeholder="Your Name"
               Text="{Binding UserName}"
               HorizontalOptions="Fill" />

        <Button Text="Start Exploring"
                Command="{Binding SaveAndContinueCommand}"
                HorizontalOptions="Fill" 
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"/>
                
    </VerticalStackLayout>
</ContentPage>
